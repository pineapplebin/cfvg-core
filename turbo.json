{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["**/*", "!.cache/**/*"],
      "outputs": ["dist/**/*"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
